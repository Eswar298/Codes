#Fit Squares os size 2x2 in a triangle

while (length > 2)
        {
            answer += (length - 2 )/2;
            length = length – 2;}     
 if d <= 2:
        print(0)
    else:
        A = d//2 - 1
        print(A*(A+1)//2)
